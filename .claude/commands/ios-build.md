# iOS Build

Build and install on the registered device.

```bash
cd ~/Documents/PrivateAI
npx expo run:ios --device "00008150-001965C41AF0401C"
```

After install: unlock phone, open app, test the specific change on device.
Check Metro console for JS errors before committing.
